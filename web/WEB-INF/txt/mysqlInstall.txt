Mysql viesion 5.7.16
               add file-->my.ini
               under [mysqld] add --> skip-grant-tables
               mysqld install
               mysqld --initialize
               net start mysql
               mysql -u root -p
               (click enter)
               type-->update MySQL.user set authentication_string=password('root') where user='root' ;
               type-->exit
               net stop mysql
               net start mysql
               mysql -u root -p
               (enter "root")
               show databases;
               if(Navicat(false)==1862){
                   set PASSWORD=PASSWORD('root')
               }
               ok --> success!